#include<stdio.h>
int main ()
{
    char ch = 'battery' ;
    if(ch='battery')
    {
        printf(" for %c watch is on");
    }
    else
    {
        (" for no %c watch is off");
    }

    return 0;
}
